# T-056 - Working-tree housekeeping

Task ID: T-056
Packet state: Done
Packet revision: 2
Project goal link: Plan/endgoal.md
Depends on: none
Related: none
Prepared from: the 2026-08-14 stub raised in `Plan/plan.md` ("Clear stale artefacts out of `App/` after checking none are packet evidence"); revision 1 expanded 2026-08-15
Expanded at: 2026-08-20
Expanded against: live `App/_to_delete/`, `App/dist/`, `App/exports/`, `App/test-results/`, `App/output/playwright/`; `App/desktop/tauri.conf.json`; `App/vite.config.ts`; `App/playwright.config.ts`; SHA-256 hashes of every candidate file; a full-tree re-grep of `Toolbox/Project-History/reports/`, `Toolbox/Project-History/evidence/`, `Toolbox/Project-History/PROJECT.md`, every file now in `Plan/Packets/` (grown from ~60 to 90+ packets since revision 1, including the full T-057–T-070d3 range), and `Plan/plan.md` (including its 2026-08-18 "Delivery 0.6.31" note)
Delivery: source gates only

## Intended Outcome

No user-visible change. `App/` contains source, configuration and current build output, and nothing else, so future sessions are not misled by stale artefacts. Every deletion this packet performs is backed by a recorded pre-deletion inventory (paths, sizes, hashes) in the execution report, so the deletion is itself evidenced even though the project keeps no Git history.

## Request Coverage

- Clear the accumulated debris out of the live application workspace.

## Verified Starting State

Re-measured 2026-08-20 (revision 1 measured 2026-08-15; two candidates have moved since then — see Corrections to revision 1 below).

| Path | Size (2026-08-20) | Contents | Regenerable how |
| --- | --- | --- | --- |
| `App/_to_delete/` | 484K (481,521 bytes across 5 files, byte-identical to revision 1) | `check.log` (352,989 B, sha256 `0d062747056b6c64f74e16238841e5bf476aa60bedc94bd1bd8798c5c72f4cb2`), `debug-mask.ts` (821 B, sha256 `8837e4fe2f386310108d8da5883c59e546b5ff45efb18aad8284a761cab9e8b6`), `NUL` (11,919 B, sha256 `79a025eaca60dd0e9ca35a87e15f0204e8f319e87789188aac625a348fbe8731`), `t.log` (5,230 B, sha256 `d9a4c7f39a04d1d1244dc77d1439a6754187b631d0dff601264894fadeb6b8ea`), `test-unit-output.log` (110,562 B, sha256 `2120e6e28a772f660eff373ab6280029195712213e00c1dc1edb9cee1687c6d3`) | Not regenerable — these are one-off dropped artefacts, not build output. |
| `App/dist/` | 25,808,076 bytes (~25M), 506 files — **changed since revision 1** (was 514 files / 25,886,739 bytes) | Vite production build output (`App/dist/assets/...`) | `vite build` (or `bun run build`, which calls it) from repo root. |
| `App/exports/` | 844K (863,466 bytes, byte-identical to revision 1) | `grid.png` (806,657 B, sha256 `a2c119ced0dbd38e6b8b6daf92c51601021e1b0c4e149dcc5bec123441b9bf58`), `test.png` (56,809 B, sha256 `a71a0b3a289d84634070556b80963f67d0a8a0d7d345e26af8d3641fd418b4d6`) | Not regenerable by a build command — these are manually exported design outputs, not derived artefacts. |
| `App/test-results/` | 45 bytes — **changed since revision 1** (was an unread 1.0K) | `.last-run.json` only, now holding real content: `{"status":"passed","failedTests":[]}` (sha256 `91d1c43004802cd49950d78eb11c8fa7d05da8ffffe219a8b13b2f561bc00903`), dated 2026-08-20 | Playwright's default `outputDir` (unset in `App/playwright.config.ts`, so it defaults to `test-results` beside the config) — regenerated by any `playwright test` run. |
| `App/output/playwright/` | 0 files (still empty) | nothing | Not referenced by any config (`grep` of `App/package.json`, all `App/*.config.ts` and `App/tests/` for `output/playwright` and `output.{0,3}playwright` returns nothing) — origin unknown, currently inert. |

## Corrections to revision 1

- **`App/dist/` was rebuilt in the interim, and this is now directly explained by project history.** File count dropped 514 → 506 and total size dropped 25,886,739 → 25,808,076 bytes. `Plan/plan.md`'s "Delivery 0.6.31 (2026-08-18, authorised by the user)" note records `bun run tauri build` running to ship that delivery — `tauri build` invokes a Vite production build first, which regenerates `App/dist/` with Vite's normal content-hashed filenames (different hashes and a slightly different file count are expected between builds of the same source, not evidence of drift). This is the same `frontendDist` target revision 1 identified — deleting it is still correctly treated as removing a regenerable build artefact, not silent-litter cleanup. Fixed Decision 2 is otherwise unchanged.
- **`App/test-results/.last-run.json` now holds real content instead of being an unread 1KB file.** It reports `{"status":"passed","failedTests":[]}` from a Playwright run today (2026-08-20) — still pure bookkeeping (no screenshots, traces or HTML report present), still regenerated automatically by the next `playwright test` invocation, and still zero-risk to delete. Fixed Decision 4 is unchanged.
- **`App/_to_delete/` and `App/exports/` are byte-identical to revision 1** (same sizes, now hash-verified) — nothing has touched either since 2026-08-15.
- **The grep re-run below covers ~30 additional packet files landed since revision 1** (T-057 through T-070d3) and reaches the identical conclusion: none of the five candidates are referenced anywhere.

## Grep evidence — the decisive question

Re-run 2026-08-20 across the current tree (`Toolbox/Project-History/reports/`, `Toolbox/Project-History/evidence/`, `Toolbox/Project-History/PROJECT.md`, every file now in `Plan/Packets/`, and `Plan/plan.md`), using the same patterns as revision 1: `_to_delete`, `debug-mask`, `check\.log`, `t\.log`, `test-unit-output\.log`, `NUL`, `App/dist`, `grid\.png`, `test\.png`, `test-results`, `output/playwright`.

| Candidate | Referenced anywhere? | Evidence |
| --- | --- | --- |
| `App/_to_delete/` (all 5 files) | **No** | Zero matches anywhere in the current tree. |
| `App/dist/` | **No genuine reference** | The literal string `App/dist` still matches only in `T-054-single-locale-reduction.md:54`, `T-008-export-formats.md:56`, `T-006-effects.md:50`, `T-002-...md:59` — the same false-positive substring inside `https://v2.tauri.App/distribute/windows-installer/` identified in revision 1 (the token `.App/dist` from `tauri.App/distribute`). No packet added since revision 1 introduces a new match. `frontendDist` in `tauri.conf.json` is a live config reference, addressed separately above; it is not "packet evidence" in the `PROJECT.md` sense. |
| `App/exports/grid.png`, `App/exports/test.png` | **No** | Zero matches for `exports/grid`, `exports/test`, or `App/exports` anywhere under `Plan/` or `Toolbox/`. |
| `App/test-results/` | **No** | `T-054-single-locale-reduction.md:54` still mentions the bare word `test-results` only inside a `grep`-exclusion instruction ("excluding `node_modules`, `dist`, `desktop/target`, `test-results` and `output`") — a generic tooling incantation, not a citation of this packet's artefacts. No newer packet references it either. |
| `App/output/playwright/` | **No** | Zero matches anywhere. |

**Conclusion, reconfirmed after five more days and ~30 more packets: none of the five candidates are referenced by any report, log or packet.** No archiving under `Toolbox/Project-History/evidence/` is needed for any of them — that step in the brief is conditional on a candidate being referenced, and none are.

## `debug-mask.ts` — read in full

`App/_to_delete/debug-mask.ts` (21 lines) is a throwaway ad-hoc script: it builds a `SceneGraph`, creates a `CANVAS` → `FRAME` → masked `ELLIPSE` + `RECTANGLE`, and `console.log`s `mask.isMask`/`mask.visible` and the SVG output of `renderNodesToSVG`. It has no test assertions, no exports, and is not imported anywhere (it sits outside `App/tests/` and outside any package's `src/`). It reads as a one-off repro used while debugging mask rendering, most plausibly for the T-013 clipping/mask work (`Plan/Packets/T-013-vector-clipping-and-layer-masks.md`, status Done). **Nothing in it is worth keeping**: it duplicates, informally and without assertions, coverage that belongs in `App/tests/engine/` if it were still needed, and T-013 is long done. No extraction is proposed.

## Fixed Decisions

1. **Delete `App/_to_delete/` in full (all 5 files).** Unreferenced by evidence (table above), and every file in it is either a log (`check.log`, `t.log`, `test-unit-output.log`), a stray shell-redirect artefact (`NUL`), or a superseded debug script (`debug-mask.ts`, see above). None are source, test, fixture or config files, so this does not cross the "no source/test/fixture/config removed" restriction.
2. **Delete `App/dist/`, but only as a build artefact, not as debris.** It is unreferenced by any report or packet, and is fully regenerable by `vite build`. Because it is the live `frontendDist` target, deletion should be called out explicitly in the execution report as "removed a regenerable build output that the desktop shell reads from — rebuild with `vite build` before the next `tauri dev`/`tauri build`" rather than silently folded in with the log-file cleanup. This packet does not run that rebuild (source gates only, no build-per-packet — see `Plan/plan.md`'s delivery policy).
3. **Delete `App/exports/grid.png` and `App/exports/test.png`.** Unreferenced by evidence. These are manually exported design outputs (not build artefacts), so "regenerable by a command" does not apply to them — they are simply stale manual exports nothing points to. If the user wants to keep either as a personal reference image, that is a call outside this packet's evidence trail; default is delete, per Stop Conditions the implementer should surface this distinction (log-litter vs. manual-export litter) in the execution report rather than treat all five directories identically.
4. **Delete `App/test-results/.last-run.json` and the empty `App/test-results/` directory it leaves behind, and delete the empty `App/output/playwright/` directory (and its empty parent `App/output/` if nothing else lives under it).** Both are unreferenced, both are either trivial Playwright bookkeeping or already-empty, and both regenerate automatically the next time Playwright runs (`testDir`/default `outputDir` behaviour confirmed in `App/playwright.config.ts`).
5. **No archiving under `Toolbox/Project-History/evidence/`.** The brief's archive-instead-of-delete proposal is conditional on a candidate being referenced-but-stale. The grep evidence table shows zero candidates are referenced, so nothing qualifies for archiving. If a future housekeeping pass finds a referenced-but-stale artefact, `Toolbox/Project-History/evidence/T-003-fixtures-20260718/` (already present) is the existing precedent to copy the shape of.
6. **Write a short "regenerable artefacts" note.** With no Git and no `.gitignore` (deliberately — do not add one), nothing stops `App/dist/`, `App/test-results/`, `App/output/playwright/`, or a future `_to_delete/`-style dumping ground from silently re-accumulating. Add a new file `App/REGENERABLE-ARTEFACTS.md` (plain prose, no code) listing: `dist/` (regenerate via `vite build`), `test-results/` and `output/playwright/` (regenerate via any Playwright run), and a one-line note that ad-hoc debug scripts belong in a session's scratch area or under `App/tests/`, never committed into `App/` itself. This is documentation only — it does not change build behaviour, does not touch `.gitignore` (there is none and none is proposed), and is the smallest artefact that satisfies "so this does not silently re-accumulate" without inventing tooling.

## Open Decisions

- **Whether to keep `App/exports/grid.png` / `test.png` as reference images rather than delete them.** Source evidence cannot settle taste — nothing points to them, but they may still be personally useful reference exports. Recommendation: delete, consistent with every other candidate, since the packet's own Stop Conditions treat "unreferenced" as the deletion signal and the user can re-export either from the live document in seconds if wanted. If the user wants them kept, drop them from step 3 at execution time — this is a one-line scope change, not a blocker.

## Allowed Changes

- Delete: `App/_to_delete/` (and its 5 files), `App/dist/`, `App/exports/grid.png`, `App/exports/test.png`, `App/test-results/.last-run.json` (and the now-empty `App/test-results/` directory), `App/output/playwright/` (and `App/output/` if it is empty afterwards).
- Create: `App/REGENERABLE-ARTEFACTS.md` (new file, prose only).

## Restrictions and Exclusions

Binding. An implementer who wants to cross one of these lines should stop and report instead.

- No source, test, fixture or configuration file is removed. Every path in Allowed Changes is a log, a stray shell artefact, a build output, a manual export image, or test-run bookkeeping — never `.ts`/`.vue`/`.rs`/`*.config.*`/`App/tests/**` content, with the single exception of `debug-mask.ts`, which was individually read and confirmed to be a non-test, non-imported throwaway script (see above).
- Nothing outside `App/` is touched, except reading (not writing) `Plan/Packets/*.md`, `Plan/plan.md`, and `Toolbox/Project-History/**` for the evidence grep.
- No `.gitignore` or any other version-control setup — the project deliberately uses none, and none is proposed here.
- `Plan/plan.md` is not edited by this packet — the parent session owns the index.
- Stop and report, do not delete, if a later re-grep at execution time turns up a genuine reference to any candidate that this expansion missed.
- Do not run `vite build`, `bun run build`, `tauri build`, `tauri dev`, `playwright test`, `bun run check`, `bun run test`, or `bun run test:unit`. This packet observes and deletes; it does not regenerate anything, per the delivery policy (source gates only, no build-per-packet).

## Implementation Steps

1. **Re-confirm the grep evidence at execution time.** Re-run the six-pattern grep (`_to_delete`, `debug-mask`, `check\.log`, `t\.log`, `test-unit-output\.log`, `App/dist`, `grid\.png`, `test\.png`, `test-results`, `output/playwright`, `NUL`) across `Toolbox/Project-History/reports/`, `Toolbox/Project-History/evidence/`, `Toolbox/Project-History/PROJECT.md`, `Plan/Packets/*.md` and `Plan/plan.md`. If anything now matches beyond the false positives already documented above, stop and report rather than deleting that candidate.
2. **Record the pre-deletion inventory.** For `App/_to_delete/*` (5 files) and `App/exports/*` (2 files), record path, byte size and a SHA-256 hash for each file individually (`sha256sum <path>` or `Get-FileHash -Algorithm SHA256 <path>` are both fine — the revision-2 expansion already computed and recorded these in Verified Starting State above; re-verify rather than trust them blind, since files can change between expansion and execution). For `App/dist/`, build a sorted manifest instead of hashing every file individually: `Get-ChildItem -Recurse -File App\dist | Sort-Object FullName | ForEach-Object { "$($_.FullName)|$((Get-FileHash $_.FullName -Algorithm SHA256).Hash)" } | Out-File dist-manifest.txt`, then hash that manifest file itself and record the single resulting hash plus the live file count and total byte size as the directory's fingerprint (506 files / 25,808,076 bytes as of the 2026-08-20 expansion — re-measure at execution time, since `App/dist/` is a live build target that may be rebuilt again before this packet executes). For `App/test-results/.last-run.json` and the two empty directories, recording path + size (45 bytes, 0 bytes, 0 bytes as of 2026-08-20) is sufficient — there is no meaningful content hash for an empty directory.
3. **Delete `App/_to_delete/` in full.**
4. **Delete `App/exports/grid.png` and `App/exports/test.png`.** Leave the (now-empty) `App/exports/` directory unless the implementer confirms nothing else references that directory path in code (a quick `grep -rn "App/exports\|exports/" App/src App/packages` — if that turns up a live export-target reference, keep the directory and only delete the two files, which is what this step already does).
5. **Delete `App/dist/`.**
6. **Delete `App/test-results/.last-run.json`, then the emptied `App/test-results/` directory; delete `App/output/playwright/`, then `App/output/` if it is empty afterward.**
7. **Create `App/REGENERABLE-ARTEFACTS.md`** per Fixed Decision 6.
8. **Write the execution report** containing the full pre-deletion inventory from step 2, the grep re-confirmation from step 1, and an explicit note that `App/dist/` was a live `frontendDist` target and needs `vite build` before the next desktop build/dev run.

## Acceptance Criteria

- [ ] `App/_to_delete/`, `App/dist/`, `App/exports/grid.png`, `App/exports/test.png`, `App/test-results/`, and `App/output/playwright/` (and `App/output/` if empty) no longer exist.
- [ ] `App/REGENERABLE-ARTEFACTS.md` exists and names `dist/`, `test-results/`, `output/playwright/` and their regeneration commands.
- [ ] The execution report contains, for every deleted file, its path, byte size, and SHA-256 hash (or, for `App/dist/`, the manifest hash plus its live file count and total size at execution time); and for the two emptied-then-removed directories, their path and size.
- [ ] The execution report re-states the grep evidence table (referenced-anywhere: no, for all five candidates) as it stood at execution time, not only at expansion time.
- [ ] No file under `App/src/`, `App/packages/*/src/`, `App/tests/`, or any `*.config.*` was touched.
- [ ] No file outside `App/` was written. `Plan/plan.md` was not edited by this packet.
- [ ] No `.gitignore` was added anywhere.

## Verification

This packet is filesystem-only; there is no code to type-check or lint. Verification is:

- `ls App` (or `Get-ChildItem App`) confirms `_to_delete/`, `dist/`, `test-results/`, `output/playwright/` are gone and `exports/` either does not exist or is empty.
- `cat App/REGENERABLE-ARTEFACTS.md` confirms the note exists and names the three regenerable paths.
- The execution report is inspected for the inventory table required by Acceptance Criteria.

No `tsgo`, `vue-tsc`, `oxlint`, `check:i18n`, `bun test` or Playwright gate applies — no source, test or config file changes in this packet.

## Stop Conditions

- Stop and report rather than delete if a re-grep at execution time turns up a genuine (non-URL-substring) reference to any candidate.
- Stop if a deletion target cannot be regenerated by a documented command and has no archived copy — this does not currently apply to any of the five candidates (see Fixed Decisions), but re-check it live before deleting `App/dist/` specifically, since it is the one candidate with a live runtime consumer (`tauri.conf.json`'s `frontendDist`).
- Stop if `App/exports/` or `App/output/` turn out to be referenced by path (not just by filename) anywhere in `App/src` or `App/packages/*/src` at execution time — re-run the directory-path grep in step 4 before deleting.

## Status record

Status: **Done**

Recorded in `Plan/plan.md` until 2026-08-18, moved here when the index was reduced to To Do / Ready / Done:

> Revision 1 (expanded 2026-08-15, with the grep-the-evidence work done during expansion, as the brief required). **Result: none of the five candidates are referenced by any report, log or packet**, so no archiving under `Toolbox/Project-History/evidence/` is needed. The only `App/dist` matches in packets are false-positive substrings of the URL `https://v2.tauri.App/distribute/...`, confirmed by match-only re-grep. Two corrections to the stub: `App/test-results/` is now just a 1KB `.last-run.json` and `App/output/playwright/` is empty — the 361K it flagged is already gone; and `App/dist/` is **not inert litter**, it is the live `frontendDist` target (`desktop/tauri.conf.json:10`), regenerable by `vite build` but leaving the shell with no frontend until rebuilt. `debug-mask.ts` read in full: a 21-line unasserted one-off mask repro, most plausibly from T-013, nothing worth keeping. Adds `App/REGENERABLE-ARTEFACTS.md` so this does not silently re-accumulate; no `.gitignore`, no VCS. One open item, a taste call: whether to keep `App/exports/grid.png` / `test.png` as reference images — default is delete.

> Revision 2 (re-expanded 2026-08-20, user-requested refresh — this packet sat Ready for five days while ~30 more packets landed, including an authorised desktop delivery). Re-measured every candidate and hashed every non-empty file. **Conclusion unchanged: none of the five candidates are referenced anywhere**, re-confirmed against a Plan/Packets tree that grew from ~60 to 90+ files since revision 1. Two facts moved: `App/dist/` was rebuilt by the 2026-08-18 "Delivery 0.6.31" `bun run tauri build` (514→506 files, 25,886,739→25,808,076 bytes — expected content-hash churn from a fresh Vite build, not drift to investigate) and `App/test-results/.last-run.json` now holds a real `{"status":"passed"}` Playwright result instead of being unread, still 45 bytes of pure bookkeeping. `App/_to_delete/` and `App/exports/` are byte-identical to revision 1, now with recorded SHA-256 hashes. No Fixed Decision, Restriction, or Stop Condition changed; only the Verified Starting State numbers, the grep re-confirmation, and the dist manifest instructions were updated for currency. The open taste call on `App/exports/*` is unresolved and still defaults to delete.

> Executed 2026-08-20. Grep evidence re-confirmed across all reports, packets, evidence, and `PROJECT.md` (0 references). Pre-deletion inventory recorded (all file hashes and dist manifest hash). `App/_to_delete/`, `App/dist/`, `App/exports/`, `App/test-results/`, and `App/output/` removed. `App/REGENERABLE-ARTEFACTS.md` created. Execution report written to `Toolbox/Project-History/reports/T-056-working-tree-housekeeping.md`.
