# Silverpoint brand sources

Both SVG masters embed the existing Silverpoint desktop artwork without redrawing it. The upstream local asset pipeline derives web, PWA and native sizes from these masters. Run `bun run generate:icons` to regenerate outputs.
